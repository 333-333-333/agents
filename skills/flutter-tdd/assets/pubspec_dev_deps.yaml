# pubspec.yaml
dev_dependencies:
  flutter_test:
    sdk: flutter
  mocktail: ^1.0.4       # Optional: for complex mock scenarios
  fpdart: ^1.1.0          # If using Either pattern (add to dependencies too)
