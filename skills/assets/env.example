# App
APP_ENV=local
HTTP_PORT=8080
GRPC_PORT=9090

# Database (provider: memory | postgres)
DB_PROVIDER=memory
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=localdev
DB_NAME=bastet_auth
DB_SSL_MODE=disable
DB_MAX_OPEN=5

# Messaging
MESSAGING_PROVIDER=nats
MESSAGING_URL=nats://localhost:4222

# Storage
STORAGE_PROVIDER=minio
STORAGE_ENDPOINT=http://localhost:9000
STORAGE_REGION=us-east-1
STORAGE_FORCE_PATH_STYLE=true

# Observability
OTEL_COLLECTOR_URL=localhost:4317

# Security
CORS_ORIGINS=*
